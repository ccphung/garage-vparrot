


<nav class="navbar navbar-expand-lg sticky-top">
    {# logo #}
    <div class="logo-container">
        <a class="nav-link" aria-current="page" href="{{ path('app_home')}}">
            <img src="{{ asset('images/logo/logo.png') }}" alt="logo" class="logo-img">
        </a>
    </div>

    {# navbar #}
    <div class="container-fluid">
        <button class="navbar-toggler navbar-dark" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="{{ path('app_home')}}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_ad')}}">Voitures d'occasion</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_contact')}}">Nous contacter</a>
                </li>
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('espace-gestion')}}">Espace gestion</a>
                    </li>
                {% endif %}
            </ul>

        {# Login and Logout buttons #}
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link logout-button" href="{{ path('app_logout')}}">Se déconnecter</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link login-button" href="{{ path('app_login')}}">Se connecter</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>